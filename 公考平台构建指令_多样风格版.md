# 公考备考平台 — 多样画面风格构建指令

> 将以下指令整体发送给 AI，即可从零构建一个公考备考平台。  
> 本版本保留原平台功能要求，并将界面从单一“蓝白专业风格”升级为“多主题、多层次、可切换、可调整”的视觉方案。

---

## 一、平台定位

构建一个公考备考平台单页 HTML 应用，覆盖国家公务员考试和省公务员考试。平台用于日常学习计划管理、时政积累、资料笔记、知识树梳理和学习数据统计。

应用采用纯前端实现，不需要后端服务器。所有数据存储在浏览器 `localStorage` 中。

### 基本要求

- 技术形态：单页 HTML 应用，桌面端优先，`min-width: 1000px`
- 数据存储：`localStorage`
- 存储 key：`gongkao-platform-v1`
- 外部依赖：无，不使用 CDN、npm 或外部库
- 界面语言：全中文
- 字体：`-apple-system, "PingFang SC", "Microsoft YaHei", system-ui, sans-serif`
- 图表：纯 SVG 实现
- 图标：内联 SVG，实现 Lucide 风格图标

### 文件结构

```text
gongkao-platform.html      主应用文件
daily_articles.js          每日时政文章数据
assets/greet-banner.jpg    首页横幅背景图，可替换
```

HTML 头部需要引入：

```html
<script src="daily_articles.js"></script>
```

---

## 二、画面风格要求

界面不要固定为单一蓝白风格，而是做成更丰富、更可变化的视觉系统。默认风格可以保持专业清爽，但需要支持多种主题切换，并让不同模块有明显的画面差异。

### 视觉方向

整体气质应体现“备考、专注、秩序、成长”，但不要做成传统后台系统的单调样式。页面可以在专业感之外加入轻量插画、渐变、卡片层次、柔和阴影、模块色彩和学习氛围元素。

界面风格需要满足：

- 首页更有仪式感，适合展示问候、倒计时、进度和今日状态
- 学习计划更偏效率工具，强调任务分组、优先级、计时器和完成反馈
- 时政积累更像阅读型内容平台，文章卡片需要有新闻感和阅读舒适度
- 资料笔记更像知识资料库，突出分类、标签、摘要和图片资料
- 知识树更偏图谱和体系化，节点层级、状态和连线要清晰
- 学习统计更偏数据看板，图表、趋势和完成率要清楚

### 多主题方案

实现一个主题配置对象，允许后续快速替换视觉风格。至少提供 4 套主题：

1. `classic`：蓝白专业风，适合默认使用
2. `warm`：米白暖色风，适合长时间学习，减少疲劳
3. `dark`：深色专注风，适合夜间学习
4. `fresh`：青绿清新风，适合轻量、明快的学习氛围

主题可以通过 `CONFIG.theme` 或页面右上角主题按钮切换。主题切换后需要立即改变 CSS 变量，并写入 `localStorage`。

参考 CSS 变量：

```css
:root {
  --page-bg: #f4f7fb;
  --surface-card: #ffffff;
  --surface-nested: #f6f8fc;
  --border: #e5e7eb;
  --border-input: #d1d5db;

  --text: #172033;
  --text-secondary: #5b657a;
  --text-tertiary: #8a94a8;

  --accent: #2563eb;
  --accent-2: #06b6d4;
  --accent-soft: #dbeafe;
  --on-accent: #ffffff;

  --success: #10b981;
  --warning: #f59e0b;
  --danger: #ef4444;
  --purple: #8b5cf6;

  --sidebar-bg: #172033;
  --sidebar-bg-top: #26324a;
  --sidebar-text: #f8fafc;

  --hero-from: #2563eb;
  --hero-to: #7c3aed;
  --hero-pattern-opacity: 0.18;
  --greet-image: url("assets/greet-banner.jpg");

  --radius-lg: 22px;
  --radius-md: 16px;
  --shadow-card: 0 14px 36px rgba(15, 23, 42, 0.08);
  --shadow-soft: 0 8px 24px rgba(15, 23, 42, 0.06);
}
```

需要在 JavaScript 中定义主题表：

```javascript
const THEMES = {
  classic: {
    name: "蓝白专业",
    vars: {
      "--page-bg": "#f0f2f5",
      "--surface-card": "#ffffff",
      "--accent": "#2563eb",
      "--accent-2": "#06b6d4",
      "--sidebar-bg": "#1e293b",
      "--sidebar-bg-top": "#334155",
      "--hero-from": "#2563eb",
      "--hero-to": "#7c3aed"
    }
  },
  warm: {
    name: "米白暖阳",
    vars: {
      "--page-bg": "#f7f1e8",
      "--surface-card": "#fffaf2",
      "--surface-nested": "#f3eadc",
      "--accent": "#c2410c",
      "--accent-2": "#f59e0b",
      "--sidebar-bg": "#3b2f2f",
      "--sidebar-bg-top": "#5b4034",
      "--hero-from": "#f97316",
      "--hero-to": "#facc15"
    }
  },
  dark: {
    name: "夜间专注",
    vars: {
      "--page-bg": "#0f172a",
      "--surface-card": "#182235",
      "--surface-nested": "#111827",
      "--border": "#29354a",
      "--text": "#f8fafc",
      "--text-secondary": "#cbd5e1",
      "--text-tertiary": "#94a3b8",
      "--accent": "#60a5fa",
      "--accent-2": "#a78bfa",
      "--sidebar-bg": "#020617",
      "--sidebar-bg-top": "#111827",
      "--hero-from": "#1d4ed8",
      "--hero-to": "#6d28d9"
    }
  },
  fresh: {
    name: "青绿清新",
    vars: {
      "--page-bg": "#eefaf6",
      "--surface-card": "#ffffff",
      "--surface-nested": "#e6f7f0",
      "--accent": "#059669",
      "--accent-2": "#14b8a6",
      "--sidebar-bg": "#064e3b",
      "--sidebar-bg-top": "#0f766e",
      "--hero-from": "#10b981",
      "--hero-to": "#22d3ee"
    }
  }
};
```

### 模块差异化设计

不同模块不要只是换文字内容，需要在卡片形态、颜色点缀和信息密度上有所区别：

- 首页：大面积渐变 hero、倒计时大数字、圆环进度和快捷入口卡片
- 学习计划：任务看板风格，分组清晰，任务卡片可用左侧色条标识科目
- 时政积累：文章阅读卡片，突出标题、来源、摘要和标签，详情弹窗排版接近阅读器
- 资料笔记：资料库卡片，摘要采用引用框样式，图片缩略图横向排列
- 知识树：树形结构要有连线、缩进、节点状态徽标，节点 hover 时有轻微浮起
- 学习统计：数据看板风格，图表卡片有标题、说明、空状态和高亮数字

### 可更改要求

代码中需要将颜色、圆角、阴影、模块色、主题名称、首页背景图等集中配置，不要把关键视觉参数散落在大量零碎样式里。

允许用户后续通过修改：

- `THEMES`
- `CONFIG.theme`
- `CONFIG.owner`
- `CONFIG.slogan`
- `CONFIG.studyGoalMin`
- `CONFIG.examDates`
- `CONFIG.subjects`
- CSS 变量
- `assets/greet-banner.jpg`

来快速改变平台的视觉风格和备考配置。

---

## 三、布局结构

页面采用左侧导航 + 右侧内容的桌面端布局。

- 左侧栏：固定宽度 `258px`，展示平台标题、口号、主题切换入口和 6 个模块导航
- 主区域：位于侧栏右侧，内容最大宽度 `1180px`
- 弹窗：居中模态框，用于编辑器、详情查看、考试日期设置等
- 卡片：统一使用圆角、边框、柔和阴影，但不同模块可有不同装饰方式
- 空状态：每个模块都要提供空状态提示，不要让页面出现空白区域

---

## 四、CONFIG 配置对象

```javascript
const CONFIG = {
  storageKey: "gongkao-platform-v1",
  owner: "公考备考",
  slogan: "GongKao Prep",
  theme: "classic",
  studyGoalMin: 180,
  examDates: { guokao: "2026-11-30", shengkao: "2027-03-15" },
  quotes: [
    "不积跬步，无以至千里；不积小流，无以成江海。",
    "宝剑锋从磨砺出，梅花香自苦寒来。",
    "行百里者半九十，坚持到最后才是胜利。",
    "业精于勤，荒于嬉；行成于思，毁于随。",
    "天下事有难易乎？为之，则难者亦易矣。",
    "锲而舍之，朽木不折；锲而不舍，金石可镂。",
    "长风破浪会有时，直挂云帆济沧海。"
  ],
  subjects: [
    { key: "言语理解", name: "言语理解", color: "var(--accent)" },
    { key: "数量关系", name: "数量关系", color: "var(--warning)" },
    { key: "判断推理", name: "判断推理", color: "var(--purple)" },
    { key: "资料分析", name: "资料分析", color: "var(--success)" },
    { key: "常识判断", name: "常识判断", color: "var(--danger)" },
    { key: "申论", name: "申论", color: "var(--accent-2)" }
  ],
  priorities: [
    { key: "P0", label: "重要", color: "#fee2e2", text: "#dc2626" },
    { key: "P1", label: "一般", color: "#fef3c7", text: "#d97706" },
    { key: "P2", label: "随手", color: "#d1fae5", text: "#059669" }
  ],
  noteCategories: ["行测", "申论", "时政", "面试", "其他"],
  checkinItems: [
    { id: 1, title: "行测刷题" },
    { id: 2, title: "申论写作" },
    { id: 3, title: "时政阅读" },
    { id: 4, title: "错题复习" }
  ],
  modules: [
    { key: "dashboard", name: "首页", icon: "home", desc: "备考总览与考试倒计时" },
    { key: "plan", name: "学习计划", icon: "list", desc: "任务清单与每日打卡" },
    { key: "current", name: "时政积累", icon: "newspaper", desc: "每日时政文章阅读" },
    { key: "notes", name: "资料笔记", icon: "pen", desc: "笔记管理与自动摘要" },
    { key: "tree", name: "知识树", icon: "tree", desc: "知识体系图谱" },
    { key: "stats", name: "学习统计", icon: "chart", desc: "数据可视化分析" }
  ]
};
```

---

## 五、六大模块规格

### 首页 dashboard

首页需要有更强的视觉氛围，顶部使用渐变 hero 卡片，可叠加背景图、半透明纹理或柔和光斑。首页展示“今天该学什么、离考试还有多久、最近状态如何”。

需要包含：

- 时钟问候卡：根据时间显示早上好、下午好、晚上好
- 实时时钟：显示时分秒、日期和当前周数
- 每日励志语：按星期从 `quotes` 数组轮换
- 考试倒计时：大号数字显示距离最近考试的剩余天数
- 考试设置：点击倒计时可设置国考、省考日期
- 4 个 SVG 进度环：计划完成率、打卡率、学习时长达成率、时政阅读量
- 4 个快捷统计卡：累计学习时长、已读时政文章数、笔记数量、知识点数量
- 4 个快捷操作：添加任务、开始学习、记笔记、看时政

### 学习计划 plan

学习计划模块要像轻量任务看板，突出今日任务、完成状态和学习计时。

需要包含：

- 任务字段：`{ id, title, subject, priority, done, estTime, actTime, date }`
- 按科目分组：言语理解、数量关系、判断推理、资料分析、常识判断、申论
- 支持添加、编辑、删除任务
- 支持按科目和优先级筛选
- 任务卡片显示标题、科目标签、优先级、预计时长、实际时长
- 每条任务有计时器按钮，支持开始和停止
- 同一时间只能有一个计时器运行
- 停止计时时，将学习记录写入 `data.studySessions`
- 每日打卡包含行测刷题、申论写作、时政阅读、错题复习
- 打卡需要计算连续天数 `streak`
- 侧栏展示今日完成数、今日学习时长、待完成任务数、本周学习趋势

### 时政积累 current

时政积累模块要像阅读型内容页，不要只做普通列表。文章卡片需要有来源、摘要、标签和阅读状态，详情弹窗要适合长文阅读。

文章数据从 `window.DAILY_ARTICLES` 加载。

数据格式：

```javascript
window.DAILY_ARTICLES = {
  date: "2026-08-11",
  articles: [
    {
      id: 1,
      title: "文章标题",
      source: "人民日报",
      date: "2026-08-11",
      url: "",
      summary: "100-200字摘要",
      content: "文章全文",
      tags: ["经济", "政策"],
      likes: 1234
    }
  ]
};
```

需要包含：

- 文章卡片：标题、来源徽标、日期、摘要、标签、阅读状态
- 今日更新徽标：当文章日期等于数据日期时显示
- 收藏功能：星标按钮
- 已读状态：点击卡片可标记为已读
- 详情弹窗：来源、日期、点赞数、标题、标签、全文内容
- 阅读原文：`url` 非空时显示按钮
- 来源筛选：全部、人民日报、人民网、半月谈、光明网、中国军网、法制日报、今日头条
- 搜索：支持搜索标题和摘要
- 侧栏统计：按来源统计文章数、阅读进度
- 无数据提示：`等待每日7点自动更新时政文章`

### 资料笔记 notes

资料笔记模块要像资料库和知识卡片集合，适合沉淀申论素材、行测技巧、时政观点和面试表达。

笔记字段：

```javascript
{
  id,
  title,
  content,
  category,
  tags,
  summary,
  images,
  date
}
```

需要包含：

- 分类：行测、申论、时政、面试、其他
- 支持新建、编辑、删除笔记
- 支持标题、内容、标签搜索
- 支持按分类筛选
- 自动摘要：抽取式算法选取得分最高的 3 句，并保持原文顺序
- 图片粘贴：在 textarea 中按 `Ctrl+V` 粘贴图片
- 图片上传：按钮选择本地图片，支持多选
- 图片存储：base64，单张建议限制 3MB
- 图片展示：编辑器缩略图、笔记卡片缩略图、点击放大查看
- 笔记卡片：标题、分类、日期、摘要、内容预览、图片缩略图、标签
- 侧栏统计：按分类统计笔记数、标签云

### 知识树 tree

知识树模块要体现层级结构和学习进度。视觉上可以使用树形连线、缩进、状态徽标和节点卡片。

预置树结构：

```text
公考知识体系
├── 行测
│   ├── 言语理解（逻辑填空、片段阅读、语句表达）
│   ├── 数量关系（数学运算、数字推理）
│   ├── 判断推理（图形推理、定义判断、类比推理、逻辑判断）
│   ├── 资料分析（增长率、比重、倍数、平均数）
│   └── 常识判断（政治、法律、经济、人文、科技）
└── 申论
    ├── 归纳概括
    ├── 综合分析
    ├── 提出对策
    ├── 贯彻执行
    └── 文章写作
```

节点数据结构：

```javascript
{
  id: "n1",
  title: "节点标题",
  status: "notstart|learning|done",
  note: "备注",
  expanded: true,
  children: []
}
```

需要包含：

- 展开和折叠节点
- 添加子节点
- 编辑节点标题和备注
- 删除节点并弹出确认
- 双击节点打开编辑器
- 掌握程度切换：未开始、学习中、已掌握
- 树形缩进和 CSS 连线
- 侧栏统计：总节点数、已掌握数、学习中数、掌握率 SVG 进度环

### 学习统计 stats

学习统计模块要像清晰的数据看板。不要堆砌太多数字，要让用户一眼看到最近趋势、薄弱科目和学习成果。

需要包含：

- 最近 7 天学习时长趋势：SVG 柱状图或折线图
- 科目分布：按 `studySessions.subject` 聚合，SVG 柱状图
- 任务完成率：按科目显示进度条
- 阅读统计：已读文章数、阅读率、按来源统计
- 笔记统计：各分类笔记数量
- 空数据状态：没有学习记录时显示提示和快捷入口

---

## 六、数据存储结构

```javascript
{
  theme: "classic",
  tasks: [
    {
      id: 101,
      title: "任务标题",
      subject: "言语理解",
      priority: "P0",
      done: false,
      estTime: 60,
      actTime: 0,
      date: "2026-08-11"
    }
  ],
  checkins: [
    {
      id: 1,
      title: "行测刷题",
      log: {
        "2026-08-11": true,
        "2026-08-10": true
      }
    }
  ],
  notes: [
    {
      id: 201,
      title: "笔记标题",
      content: "内容",
      category: "行测",
      tags: ["标签1"],
      summary: "摘要",
      images: ["data:image/png;base64,..."],
      date: "2026-08-11"
    }
  ],
  tree: {
    id: "n1",
    title: "公考知识体系",
    status: "done",
    note: "",
    expanded: true,
    children: []
  },
  studySessions: [
    {
      date: "2026-08-11",
      subject: "言语理解",
      minutes: 45
    }
  ],
  articles: {
    readIds: [1, 3],
    favIds: [2]
  },
  examDates: {
    guokao: "2026-11-30",
    shengkao: "2027-03-15"
  }
}
```

---

## 七、图标系统

所有图标使用内联 SVG，风格接近 Lucide，`24x24`，`stroke-based`，`stroke-width=1.7`。

需要图标：

```text
home, list, newspaper, pen, tree, chart, plus, search, trash, close,
check, chevronRight, chevronDown, edit, clock, calendar, target, flame,
star, bookmark, play, pause, settings, timer, book, fileText, filter, tag,
brain, trendUp, award, graduationCap, external, sun, moon, bolt, image,
checkCircle, mapPin, quote, palette
```

图标函数：

```javascript
function icon(name, size = 22, sw = 1.7) {
  const path = ICONS[name] || ICONS.chart;
  return `<svg viewBox="0 0 24 24" width="${size}" height="${size}" fill="none"
    stroke="currentColor" stroke-width="${sw}"
    stroke-linecap="round" stroke-linejoin="round">${path}</svg>`;
}
```

---

## 八、daily_articles.js 文件格式

每篇文章必须对应前一天某一篇**具体文章**，`url` 必须是该文章在原文网站的精确页面链接（如 `http://opinion.people.com.cn/n1/2026/0811/c461529-40777125.html`），不能填网站主页（如 `https://www.people.com.cn/`）。`summary` 是单篇文章的摘要，不是对前一天所有新闻的概括。`content` 是该文章的全文内容。

```javascript
window.DAILY_ARTICLES = {
  date: "2026-08-12",
  articles: [
    {
      id: 1,
      title: "文章标题",
      source: "人民网",
      date: "2026-08-11",
      url: "http://opinion.people.com.cn/n1/2026/0811/c461529-40777125.html",
      summary: "100-200字摘要，概括该文章的核心观点",
      content: "文章全文内容",
      tags: ["经济", "政策"],
      likes: 0
    }
  ]
};
```

关键规则：
- `url` 必须精确到文章本身，不能是网站首页或频道页
- 如果文章无原文链接，`url` 填空字符串，卡片上"阅读原文"按钮点击时提示"该文章暂无原文链接"
- 至少 10 篇文章，id 从 1 到 10

---

## 九、技术约束

1. 使用纯 HTML、CSS、JavaScript 实现
2. 所有代码尽量写在 `gongkao-platform.html` 一个文件中
3. 不引入 CDN、npm 包或外部库
4. 数据全部存储在 `localStorage`
5. 所有图表使用纯 SVG
6. 所有图标使用内联 SVG
7. 桌面端优先，最小宽度 `1000px`
8. `daily_articles.js` 未加载时显示空状态，不影响其他模块
9. 所有用户输入必须通过 `esc()` 函数转义后再渲染，避免 XSS
10. 主题切换、考试日期、学习数据都要持久化保存
11. 页面刷新后应恢复上次主题、任务、笔记、知识树和阅读状态

---

## 十、完整构建指令

```text
构建一个公考备考平台单页 HTML 应用，覆盖国考和省考备考场景。

技术要求：
使用纯前端 HTML、CSS、JavaScript 实现，无外部依赖，不使用 CDN、npm 或外部库。数据存储在 localStorage，key 为 gongkao-platform-v1。界面为全中文，桌面端优先，min-width: 1000px。所有图表用纯 SVG 实现，所有图标用内联 SVG 实现，图标风格接近 Lucide。

文件要求：
生成 gongkao-platform.html 主应用文件，并生成 daily_articles.js 示例数据文件。HTML 头部通过 <script src="daily_articles.js"></script> 引入文章数据。首页背景图变量使用 --greet-image: url("assets/greet-banner.jpg")，图片不存在时用渐变背景兜底。

画面风格：
不要固定为单一蓝白后台风格。请实现多主题、多画面层次、可切换、可调整的视觉系统。默认主题为蓝白专业风，同时提供米白暖阳、夜间专注、青绿清新 3 套主题。主题通过 THEMES 对象和 CSS 变量控制，支持页面按钮切换，切换结果写入 localStorage。不同模块需要有不同画面气质：首页更有仪式感，学习计划像任务看板，时政积累像阅读内容平台，资料笔记像知识资料库，知识树像体系图谱，学习统计像数据看板。

布局：
左侧固定侧栏，宽 258px，展示平台标题、口号、主题切换入口和 6 个模块导航。右侧为主内容区，max-width 1180px。弹窗用于任务编辑、笔记编辑、文章详情、节点编辑、考试日期设置等。所有卡片使用圆角、边框、柔和阴影，但不同模块可以使用不同装饰方式。

配置：
定义 CONFIG 对象，包含 storageKey、owner、slogan、theme、studyGoalMin、examDates、quotes、subjects、priorities、noteCategories、checkinItems、modules。定义 THEMES 对象，至少包含 classic、warm、dark、fresh 四个主题，每个主题包含名称和 CSS 变量。颜色、圆角、阴影、模块色、主题名、首页背景图等关键视觉参数需要集中配置，方便后续修改。

六大模块：

1. 首页 dashboard
包含渐变 hero 问候卡、实时时钟、日期、当前周数、每日励志语、考试倒计时、考试日期设置、4 个 SVG 进度环、4 个快捷统计卡和 4 个快捷操作按钮。倒计时显示距离最近考试的剩余天数，支持国考和省考日期配置。

2. 学习计划 plan
包含任务清单和每日打卡。任务字段为 id、title、subject、priority、done、estTime、actTime、date。任务按行测五大模块和申论分组，支持添加、编辑、删除、筛选。每条任务有开始和停止计时按钮，同一时间只能运行一个计时器。停止后计算实际学习分钟数，写入 studySessions。每日打卡包含行测刷题、申论写作、时政阅读、错题复习，并计算连续打卡天数。

3. 时政积累 current
从 window.DAILY_ARTICLES 加载文章。文章卡片展示标题、来源、日期、摘要、标签、已读状态和收藏状态。支持按来源筛选、标题和摘要搜索、详情弹窗阅读全文。url 非空时显示阅读原文按钮。没有数据时显示“等待每日7点自动更新时政文章”。

4. 资料笔记 notes
支持创建、编辑、删除笔记。笔记字段包含 id、title、content、category、tags、summary、images、date。分类包含行测、申论、时政、面试、其他。支持标题、内容、标签搜索，支持分类筛选。实现抽取式自动摘要，按句子切分、词频打分、选取得分最高 3 句并保持原文顺序。支持 Ctrl+V 粘贴图片和按钮上传图片，图片以 base64 保存，卡片显示缩略图，点击可放大。

5. 知识树 tree
预置公考知识体系，包括行测下的言语理解、数量关系、判断推理、资料分析、常识判断，以及申论下的归纳概括、综合分析、提出对策、贯彻执行、文章写作。节点字段包含 id、title、status、note、expanded、children。支持展开折叠、添加子节点、编辑节点、删除节点、双击编辑、掌握程度切换。状态包含未开始、学习中、已掌握。使用树形缩进和 CSS 连线表现层级。

6. 学习统计 stats
展示最近 7 天学习时长趋势、科目学习时长分布、任务完成率、阅读统计和笔记分类统计。图表使用 SVG 实现。没有数据时显示空状态提示和快捷入口。

数据存储：
localStorage 中保存 theme、tasks、checkins、notes、tree、studySessions、articles、examDates。页面刷新后恢复上次主题和所有学习数据。

安全要求：
所有用户输入渲染前必须通过 esc() 函数转义，防止 XSS。删除操作需要确认弹窗。图片大小需要限制，避免 localStorage 过大。

输出要求：
请直接输出完整可运行代码。生成 gongkao-platform.html 和 daily_articles.js。代码结构清晰，CSS 变量和 THEMES 配置集中，方便后续更改画面风格。
```
